# metadata_truth.yaml — agent-layer ground truth for the finance corpus.
# GENERATED by dataraum-testdata (src/testdata/metadata_truth.py); do not hand-edit.
# Table/column names are remapped to match this run's exported schema.
vertical: finance
metric_additivity:
  metrics:
    active_accounts:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: distinct_count
      time_reason: distinct_count
      note: COUNT(DISTINCT account) — the distinct set overlaps across slices.
    average_transaction_value:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: average
      time_reason: average
      note: AVG — an average of averages does not reconcile on any axis.
    current_ratio:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: current_assets / current_liabilities — a ratio; non-additive everywhere.
    gross_margin:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: a margin (X / revenue) — a ratio; non-additive on every axis.
    ebitda_margin:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: a margin (X / revenue) — a ratio; non-additive on every axis.
    net_margin:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: a margin (X / revenue) — a ratio; non-additive on every axis.
    operating_margin:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: a margin (X / revenue) — a ratio; non-additive on every axis.
    dso:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: days-sales-outstanding — a ratio (AR / revenue × days); non-additive.
    dpo:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: ratio
      time_reason: ratio
      note: days-payable-outstanding — a ratio (AP / COGS × days); non-additive.
    transaction_count:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: COUNT over the journal_lines event fact — additive (needs event-grain).
    gross_profit:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: a sum/difference of flow measures — additive on both axes (needs flow
        labels).
    operating_income:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: a sum/difference of flow measures — additive on both axes (needs flow
        labels).
    ebitda:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: a sum/difference of flow measures — additive on both axes (needs flow
        labels).
    net_income:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: a sum/difference of flow measures — additive on both axes (needs flow
        labels).
  measures:
    account:
      determinism: function_symmetry
      categorical_additive: false
      time_additive: false
      categorical_reason: distinct_count
      time_reason: distinct_count
      note: The active_accounts extract — COUNT(DISTINCT), grounded by one metric.
    revenue:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    cost_of_goods_sold:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    operating_expense:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    depreciation:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    interest:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    tax:
      determinism: label_dependent
      categorical_additive: true
      time_additive: true
      categorical_reason: null
      time_reason: null
      note: SUM of an income-statement flow — additive on both axes (needs a flow
        label).
    current_assets:
      determinism: label_dependent
      categorical_additive: true
      time_additive: false
      categorical_reason: null
      time_reason: stock
      note: SUM of a balance-sheet stock — reconciles across categories, time-stripped.
    current_liabilities:
      determinism: label_dependent
      categorical_additive: true
      time_additive: false
      categorical_reason: null
      time_reason: stock
      note: SUM of a balance-sheet stock — reconciles across categories, time-stripped.
    accounts_receivable:
      determinism: label_dependent
      categorical_additive: true
      time_additive: false
      categorical_reason: null
      time_reason: stock
      note: SUM of a balance-sheet stock — reconciles across categories, time-stripped.
    accounts_payable:
      determinism: label_dependent
      categorical_additive: true
      time_additive: false
      categorical_reason: null
      time_reason: stock
      note: SUM of a balance-sheet stock — reconciles across categories, time-stripped.
    transaction_amount:
      determinism: label_dependent
      categorical_additive: false
      time_additive: false
      categorical_reason: average
      time_reason: average
      note: Grounded by BOTH transaction_count (COUNT) and average_transaction_value
        (AVG); the measure verdict is the deduped most-restrictive fold across metrics,
        so AVG wins — proves the shared-field fold end-to-end.
stock_flow:
  journal_lines.debit: additive
  journal_lines.credit: additive
  journal_lines.net_amount: additive
  invoices.amount: additive
  payments.amount: additive
  bank_transactions.amount: additive
  trial_balance.debit_balance: additive
  trial_balance.credit_balance: additive
  balance_sheet.ending_balance: point_in_time
  fx_rates.rate: point_in_time
reconciles_structurally:
- trial_balance.debit_balance
- trial_balance.credit_balance
- balance_sheet.ending_balance
relationships:
- from: journal_lines.entry_id
  to: journal_entries.entry_id
- from: journal_lines.account_id
  to: chart_of_accounts.account_id
- from: invoices.entry_id
  to: journal_entries.entry_id
- from: payments.invoice_id
  to: invoices.invoice_id
- from: bank_transactions.account_id
  to: chart_of_accounts.account_id
- from: bank_transactions.payment_id
  to: payments.payment_id
- from: trial_balance.account_id
  to: chart_of_accounts.account_id
- from: balance_sheet.account_id
  to: chart_of_accounts.account_id
- from: chart_of_accounts.parent_id
  to: chart_of_accounts.account_id
table_roles:
  facts:
  - journal_lines
  - invoices
  - payments
  - bank_transactions
  - trial_balance
  - balance_sheet
  dimensions:
  - chart_of_accounts
  ambiguous:
  - journal_entries
  - fx_rates
semantic_roles:
  measure:
  - journal_lines.debit
  - journal_lines.credit
  - journal_lines.net_amount
  - invoices.amount
  - payments.amount
  - bank_transactions.amount
  - trial_balance.debit_balance
  - trial_balance.credit_balance
  - balance_sheet.ending_balance
  - fx_rates.rate
  timestamp:
  - journal_entries.date
  - invoices.date
  - invoices.due_date
  - payments.date
  - bank_transactions.date
  - fx_rates.date
  - trial_balance.period
  - balance_sheet.period
business_concepts:
  required:
    journal_lines.debit: debit
    journal_lines.credit: credit
    trial_balance.debit_balance: account_balance
    trial_balance.credit_balance: account_balance
    balance_sheet.ending_balance: account_balance
    invoices.amount: transaction_amount
    payments.amount: transaction_amount
    bank_transactions.amount: transaction_amount
cycles:
- canonical_type: journal_entry_cycle
  key_tables:
  - journal_entries
  - journal_lines
  required: true
- canonical_type: accounts_payable
  key_tables:
  - invoices
  - payments
  required: true
- canonical_type: bank_reconciliation
  key_tables:
  - bank_transactions
  - payments
  required: true
- canonical_type: period_close
  key_tables:
  - trial_balance
  - balance_sheet
  required: false
folded_dimensions: []
degenerate_ids: []
